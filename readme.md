# Box Defect Detection using YOLO and ZED Camera

This project detects **damaged boxes** and **good boxes** in real time using a **YOLO object detection model** and a **ZED camera**. The main Python script for running the detection system is:

Install PyZED separately:
cd /usr/local/zed
python3 get_python_api.py
pip install pyzed-*.whl


```bash
defect_detection.py
